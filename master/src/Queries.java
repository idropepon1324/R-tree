import java.util.*;

public class Queries {

    private Optional<? extends TreeNode> root;

    public Queries(){
        root = null;
    }

    public List<? extends TreeNode> skyLineBBS(TreeNode root){
       List<? extends TreeNode> skyLineList;
       skyLineList = null;

       //ftiaxno heap


       return skyLineList;
    }

    private List<? extends TreeNode> skyLineBBShelper(TreeNode node){
        heap h;
        List<? extends TreeNode> skyLineList;

        if(node exi pedia){
            h.insert(paidia tou node)
        }

        if(h is empty){
            return skyLineList;
        }else{

        }
    }
}
